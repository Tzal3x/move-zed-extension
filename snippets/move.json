{
    "function": {
        "prefix": "fun",
        "body": [
            "fun ${1:FUNCTION_NAME}(${2:ARGUMENTS}): ${3:RETURN_TYPE} {",
            "    ${4: IMPLEMENTATION}",
            "}$0"
        ],
        "description": "Creates a generic function"
    },
    "one_time": {
        "prefix": "init",
        "body": [
            "/// The One Time Witness (OTW) type should be named after the module with all uppercase letters.",
            "/// Only `drop`, no fields, no generics, all uppercase.",
            "public struct ${1:MODULE_NAME_ALL_CAPS} has drop {}",
            "",
            "fun init(otw: OTW, ctx: &mut TxContext) {",
            "    // do something with the OTW",
            "    ${2:TODO}$0",
            "}"
        ],
        "description": "Defines the init function including a One Time Witness type."
    },
    "enum": {
      "prefix": "enum",
      "body": [
          "public enum ${1:NAME} {",
          "    ${2:VARIANT_1} { ${3:TODO} },",
          "    ${4:VARIANT_2} { ${5:TODO} },",
          "}$0"
      ],
      "description": "Defines an enum type with variants."
    },
    "struct": {
        "prefix": "struct",
        "body": ["public struct ${1:NAME} has ${2:ABILITIES} { ${3:TODO} }$0"],
        "description": "Defines a public struct."
    },
    "struct with key definition": {
        "prefix": "struct-has-key",
        "body": ["public struct ${1:NAME} has key { id: UID }"],
        "description": "Defines a public struct with the \"key\" ability."
    },
    "if-else block": {
        "prefix": "ifelse",
        "body": ["if (${1:CONDITION}){\n    ${2:TODO}\n} else {\n    ${3:TODO}\n}$0"],
        "description": "Evaluates if a condition is true or false."
    },
    "if-else-if block": {
        "prefix": "ifelseif",
        "body": [
            "if (${1:CONDITION}){\n    ${2:TODO}\n} else if (${2:CONDITION}){\n    ${2:TODO}\n}$0"
        ],
        "description": "Evaluates if a certain condition is true, otherwise it evaluates another one."
    },
    "loop": {
        "prefix": "loop",
        "body": ["loop {\n    if (${1:CONDITION}) break;\n};$0"],
        "description": "Executes a block of code repeatedly as long as the condition is true."
    },
    "loop-labeled": {
        "prefix": "loop-labeled",
        "body": ["'a: loop {\n    if (${1:CONDITION}) break 'a;\n    ${2:TODO}\n};$0"],
        "description": "Executes a labeled loop, allowing for breaking out of the loop with the label."
    },
    "loops-labeled-nested": {
        "prefix": "loops-labeled-nested",
        "body": [
            "'outer: loop {\n    'inner: loop {\n        if (${1:CONDITION}) break 'outer;\n        if (${2:CONDITION}) break 'inner;\n        ${3:TODO}\n    }\n    ${4:TODO}\n};$0"
        ],
        "description": "Executes nested labeled loops, allowing for breaking out of specific loops using their labels."
    },
    "while": {
        "prefix": "while",
        "body": ["let i = 0;\nwhile (i < ${1:N}) {\n    ${2:TODO}\n};$0"],
        "description": "Executes a block of code repeatedly as long as the condition is true."
    },
    "while-labeled": {
        "prefix": "while-labeled",
            "body": ["let i = 0;\n'a: while (i < ${1:N}) {\n    if (${2:CONDITION}) break 'a\n};$0"],
        "description": "Executes a block of code repeatedly as long as the condition is true."
    }
}
